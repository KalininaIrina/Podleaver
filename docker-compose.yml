services:
  mysql:
    image: mysql:8.0
    container_name: sql8_container
    restart: always
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "060309"
      MYSQL_DATABASE: "tracker"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: